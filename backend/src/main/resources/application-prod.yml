# Prod profile — public AWS deployment
# Activate with: SPRING_PROFILES_ACTIVE=prod

logging:
  level:
    root: WARN
    com.worknear: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false

server:
  error:
    include-message: never
    include-binding-errors: never

worknear:
  otp:
    mock: ${OTP_MOCK:false}
  payment:
    mock: ${PAYMENT_MOCK:false}
  storage:
    mock: ${STORAGE_MOCK:false}
  push:
    mock: ${PUSH_MOCK:false}
